# Base URL of the FastAPI control plane. Defaults to http://localhost:8000
# in lib/api.ts if unset, matching `uvicorn main:app --app-dir control-plane`.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
